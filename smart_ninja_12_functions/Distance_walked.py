def number_steps(distance, length):
    num_steps = distance/length
    return num_steps


# distance_km = float(input("distance in kilometers:"))
# length = float(input("stride length in meters:"))
# value = number_steps(distance_km * 1000, length)
# print(value)
#
# distance_km = float(input("distance in km:"))
# length = float(input("stride length in meters:"))
# value = number_steps(distance_km*1000, length)
# print(value)


